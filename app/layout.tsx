import type { Metadata } from 'next'
import { Inter, Playfair_Display } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter'
})

const playfair = Playfair_Display({ 
  subsets: ["latin"],
  variable: '--font-playfair'
})

export const metadata: Metadata = {
  title: 'Atlantic Airlines - Vuela con estilo',
  description: 'Reserva tus vuelos con Atlantic Airlines. Destinos internacionales, servicio premium y las mejores tarifas para tu próximo viaje.',
  generator: 'Atlantic Airlines',
  icons: {
    icon: '/img/logo.svg',
    shortcut: '/img/logo.svg',
    apple: '/img/logo.svg',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="es" className="bg-background">
      <body className={`${inter.variable} ${playfair.variable} font-sans antialiased`}>
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
